name: Keep Alive

on:
  schedule:
    - cron:  '*/15 * * * *'  # Every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping all my apps
        run: |
          curl -s https://pet-adoption-backend-00tu.onrender.com/health
          curl -s https://movies-backend-rmbg.onrender.com/health
         
